<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/softclones"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavBotDialog"
        tools:targetApi="31">
        <activity
            android:name=".UserMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".log_in"> </activity>
        <activity android:name=".AdminMainActivity"></activity>
        <activity android:name=".signUp"></activity>
        <activity android:name=".Registration"></activity>
        <activity android:name=".volunteermodule.MainActivity"></activity>
    </application>

</manifest>